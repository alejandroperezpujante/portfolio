---
import type { HTMLTag } from 'astro/types';

interface Props {
	headingLevel?: HTMLTag
}

const { headingLevel: Element = 'h3' } = Astro.props;
---

<Element class="mb-2 text-3xl font-bold">
	<slot />
</Element>
