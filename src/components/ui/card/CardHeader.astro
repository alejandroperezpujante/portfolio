---
import { cn } from "../../../utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<header class={cn("flex flex-col gap-1.5 p-6", className)}>
  <slot />
</header>
